[Unit]
Description=Monitor Arduino upload — libère le port série avant upload IDE
After=serial-relay.service

[Service]
Type=simple
ExecStart=/usr/local/bin/relay-upload-monitor
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
